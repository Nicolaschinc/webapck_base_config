{
    // Default options
    'functions': true,
    'variableNameTransforms': ['dasherize']
}

{{#block "sprites"}}
{{#each sprites}}
@mixin {{../spritesheet_info.strings.name}}_{{strings.name}}{background-image: url('{{escaped_image}}');background-size:}
@mixin {{../spritesheet_info.strings.name}}_{{strings.name}}: ({{px.x}}, {{px.y}}, {{px.offset_x}}, {{px.offset_y}}, {{px.width}}, {{px.height}}, {{px.total_width}}, {{px.total_height}}, '{{{escaped_image}}}', '{{name}}');
{{/each}}
{{/block}}